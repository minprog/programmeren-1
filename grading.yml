templates:
    5: &exam
        type: pass
        subgrades:
            pass: boolean
        calculation: pass
        hide_calculated: true

grades:
    hertentamen2:
        <<: *exam
        deadline: 28/3/24 14:00
        exam: true
        files:
            required:
                'print_blokken.c': |
                    #include <stdio.h>
                    #include <cs50.h>

                    void print_blokken(int aantal_blokken, int breedte)
                    {
                        // TODO
                    }

                    // !! main mag niet aangepast worden !!
                    int main(void)
                    {
                        print_blokken(2, 3);
                        print_blokken(1, 5);
                        print_blokken(3, 4);
                    }
                'bevat.c': |
                    #include <stdio.h>
                    #include <cs50.h>
                    #include <string.h>
                    #include <ctype.h>

                    bool bevat(string tekst, string tekens)
                    {
                        // TODO
                    }

                    // !! main mag niet aangepast worden !!
                    int main(void)
                    {
                        // dit print: 1
                        printf("%d\n", bevat("hello, world!", "e"));

                        // dit print: 0
                        printf("%d\n", bevat("hello, world!", "xyz"));

                        // let op de hoofdletters
                        // dit print: 1
                        printf("%d\n", bevat("SpOnGeBoB", "abc"));

                        // let op de hoofdletters
                        // dit print: 1
                        printf("%d\n", bevat("spongebob", "ABC"));

                        // dit print: 1
                        printf("%d\n", bevat("Niet zo luid!", "?!"));

                        // dit print: 0
                        printf("%d\n", bevat("okee", "?!"));
                    }
                'som_zonder_uitschieters.c': |
                    #include <stdio.h>
                    #include <cs50.h>

                    int som_zonder_uitschieters(int getallen[], int n)
                    {
                        // TODO
                    }

                    // !! main mag niet aangepast worden !!
                    int main(void)
                    {
                        // het gemiddelde is 4.0, dus 10 is een uitschieter, dit print: 14
                        int getallen1[] = {1, 2, 3, 4, 10, 4};
                        printf("%d\n", som_zonder_uitschieters(getallen1, 6));

                        // het gemiddelde is 200.0, er zijn geen uitschieters, dit print: 600
                        int getallen2[] = {100, 200, 300};
                        printf("%d\n", som_zonder_uitschieters(getallen2, 3));

                        // het gemiddelde is 370.0, dus 1000 is een uitschieter, dit print: 110
                        int getallen3[] = {10, 100, 1000};
                        printf("%d\n", som_zonder_uitschieters(getallen3, 3));
                    }
                'tel_woorden_van_lengte.c': |
                    #include <stdio.h>
                    #include <cs50.h>
                    #include <string.h>
                    #include <ctype.h>

                    int tel_woorden_van_lengte(string tekst, int lengte)
                    {
                        // TODO
                    }

                    // !! main mag niet aangepast worden !!
                    int main(void)
                    {
                        // dit print: 2
                        printf("%d\n", tel_woorden_van_lengte("hello world", 5));

                        // dit print: 1
                        printf("%d\n", tel_woorden_van_lengte("u de het vier groot groter grootst", 4));

                        // dit print: 3
                        printf("%d\n", tel_woorden_van_lengte("let op hierin staan drie woorden van een bepaalde lengte", 3));
                    }
                'print_top_drie.c': |
                    #include <stdio.h>
                    #include <cs50.h>

                    void print_top_drie(int getallen[], int n)
                    {
                        // TODO
                    }

                    // !! main mag niet aangepast worden !!
                    int main(void)
                    {
                        // dit print: 5, 4, 3
                        int getallen1[] = {1, 2, 3, 4, 5};
                        print_top_drie(getallen1, 5);

                        // dit print: 6, 5, 4
                        int getallen2[] = {6, 5, 4, 3};
                        print_top_drie(getallen2, 4);

                        // dit print: 55, 40, 30
                        int getallen3[] = {30, 20, 40, 10, 15, 55};
                        print_top_drie(getallen3, 6);
                    }
